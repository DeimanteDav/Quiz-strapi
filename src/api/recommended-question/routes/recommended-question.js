'use strict';

/**
 * recommended-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::recommended-question.recommended-question');
